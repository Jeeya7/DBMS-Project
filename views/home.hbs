<section class="home-index">
  <div class="container">
    <h1>Home Page</h1>
    <p>Welcome to the Rocket Event Management System. Use the navigation above to explore students, events, and locations.</p>

    <ul class="index-list">
      {{#each pages}}
        <li>
          <a href="{{this.url}}">{{this.name}}</a>
          {{#if this.description}} — <small>{{this.description}}</small>{{/if}}
        </li>
      {{/each}}
    </ul>
  </div>
</section>